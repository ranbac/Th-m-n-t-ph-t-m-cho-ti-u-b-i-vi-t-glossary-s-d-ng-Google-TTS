<?php
/**
 * =================================================================================
 * PHIÊN BẢN CUỐI CÙNG: GIAO DIỆN ADMIN, BẢO MẬT, CACHE & TƯƠNG THÍCH IOS
 * =================================================================================
 * Chức năng: Thêm nút phát âm cho 'glossary' và trang cài đặt để quản lý API Key.
 * API Key được lưu vào database một cách an toàn.
 * File âm thanh được cache lại để tiết kiệm chi phí và tăng tốc độ.
 */

// -----------------------------------------------------------------------------
// PHẦN 1: THÊM NÚT BẤM VÀO NỘI DUNG BÀI VIẾT (Không thay đổi)
// -----------------------------------------------------------------------------
function them_nut_doc_tieu_de_glossary_v3($content) {
    if ( is_singular('glossary') ) {
        $nut_bam_html = '
            <p>
                <strong>Phát âm</strong> 
                <button id="google-tts-button" 
                        data-post-id="' . get_the_ID() . '" 
                        style="cursor: pointer; border: none; background: none; font-size: 1.5em; vertical-align: middle; padding: 0;">
                        🔊
                </button>
                <span id="google-tts-status" style="font-size: 0.8em; margin-left: 8px;"></span>
            </p>
            <hr>';
        return $nut_bam_html . $content;
    }
    return $content;
}
add_filter('the_content', 'them_nut_doc_tieu_de_glossary_v3');

// -----------------------------------------------------------------------------
// PHẦN 2: THÊM JAVASCRIPT VÀO FOOTER (Không thay đổi)
// -----------------------------------------------------------------------------
function them_script_cho_nut_doc_tieu_de_v3() {
    if ( is_singular('glossary') ) {
        wp_register_script('google-tts-script', false);
        wp_enqueue_script('google-tts-script');
        wp_localize_script('google-tts-script', 'tts_ajax_object', [
            'ajax_url' => admin_url('admin-ajax.php'),
            'nonce'    => wp_create_nonce('google_tts_nonce')
        ]);
        ?>
        <script type="text/javascript" id="google-tts-script-inline">
            document.addEventListener('DOMContentLoaded', function() {
                const playButton = document.getElementById('google-tts-button');
                const statusLabel = document.getElementById('google-tts-status');
                const audioPlayer = new Audio();

                if (playButton) {
                    playButton.addEventListener('click', async function() {
                        const postId = this.getAttribute('data-post-id');
                        audioPlayer.play().catch(e => {});
                        statusLabel.textContent = 'Đang tải...';
                        playButton.disabled = true;

                        const formData = new FormData();
                        formData.append('action', 'get_google_tts_audio');
                        formData.append('post_id', postId);
                        formData.append('nonce', tts_ajax_object.nonce);

                        try {
                            const response = await fetch(tts_ajax_object.ajax_url, {
                                method: 'POST',
                                body: formData
                            });
                            const data = await response.json();
                            if (data.success) {
                                audioPlayer.src = data.data.audioUrl;
                                audioPlayer.play();
                                statusLabel.textContent = '';
                            } else {
                                throw new Error(data.data.message || 'Có lỗi xảy ra.');
                            }
                        } catch (error) {
                            console.error('Lỗi khi xử lý TTS:', error);
                            statusLabel.textContent = error.message;
                        } finally {
                            playButton.disabled = false;
                        }
                    });
                }
            });
        </script>
        <?php
    }
}
add_action('wp_footer', 'them_script_cho_nut_doc_tieu_de_v3');

// -----------------------------------------------------------------------------
// PHẦN 3: XỬ LÝ AJAX PHÍA SERVER (Cập nhật để lấy API Key từ database)
// -----------------------------------------------------------------------------
function xu_ly_tts_ajax_v3() {
    if (!isset($_POST['nonce']) || !wp_verify_nonce($_POST['nonce'], 'google_tts_nonce')) {
        wp_send_json_error(['message' => 'Yêu cầu không hợp lệ!'], 403);
        return;
    }

    if (!isset($_POST['post_id']) || !($post_id = intval($_POST['post_id']))) {
        wp_send_json_error(['message' => 'Thiếu Post ID.'], 400);
        return;
    }
    $text_to_read = get_the_title($post_id);

    $upload_dir = wp_upload_dir();
    $cache_dir_path = $upload_dir['basedir'] . '/google-tts-cache';
    $cache_dir_url = $upload_dir['baseurl'] . '/google-tts-cache';
    if (!file_exists($cache_dir_path)) {
        wp_mkdir_p($cache_dir_path);
    }
    $file_name = md5($text_to_read) . '.mp3';
    $file_path = $cache_dir_path . '/' . $file_name;
    $file_url = $cache_dir_url . '/' . $file_name;

    if (file_exists($file_path)) {
        wp_send_json_success(['audioUrl' => $file_url]);
        return;
    }

    // *** THAY ĐỔI QUAN TRỌNG: Lấy API Key từ option đã lưu trong database ***
    $api_key = get_option('google_tts_api_key');
    if (empty($api_key)) {
        wp_send_json_error(['message' => 'Lỗi: API Key chưa được cấu hình.'], 500);
        return;
    }
    
    $api_url = 'https://texttospeech.googleapis.com/v1/text:synthesize?key=' . $api_key;
    $request_body = [
        'input' => ['text' => $text_to_read],
        'voice' => ['languageCode' => 'cmn-CN', 'name' => 'cmn-CN-Standard-A'],
        'audioConfig' => ['audioEncoding' => 'MP3', 'speakingRate' => 0.8]
    ];
    $response = wp_remote_post($api_url, [
        'headers' => ['Content-Type' => 'application/json; charset=utf-8'],
        'body' => json_encode($request_body),
        'method' => 'POST',
        'timeout' => 15,
    ]);

    if (is_wp_error($response)) {
        wp_send_json_error(['message' => 'Lỗi kết nối đến Google API.'], 500);
        return;
    }

    $response_body = json_decode(wp_remote_retrieve_body($response), true);
    if (isset($response_body['audioContent'])) {
        $audio_content = base64_decode($response_body['audioContent']);
        file_put_contents($file_path, $audio_content);
        wp_send_json_success(['audioUrl' => $file_url]);
    } else {
        $error_message = $response_body['error']['message'] ?? 'Lỗi không xác định từ Google API.';
        wp_send_json_error(['message' => $error_message], 500);
    }
}
add_action('wp_ajax_get_google_tts_audio', 'xu_ly_tts_ajax_v3');
add_action('wp_ajax_nopriv_get_google_tts_audio', 'xu_ly_tts_ajax_v3');

// -----------------------------------------------------------------------------
// PHẦN 4: TẠO GIAO DIỆN ADMIN ĐỂ NHẬP API KEY (Mới)
// -----------------------------------------------------------------------------

/**
 * Thêm một trang cài đặt vào menu "Cài đặt" (Settings)
 */
function google_tts_register_settings_page() {
    add_options_page(
        'Google TTS Settings',          // Tiêu đề trang
        'Google TTS Settings',          // Tên menu
        'manage_options',               // Quyền truy cập
        'google-tts-settings-slug',     // Slug của trang
        'google_tts_settings_page_html' // Hàm để render HTML
    );
}
add_action('admin_menu', 'google_tts_register_settings_page');

/**
 * Hàm render HTML cho trang cài đặt
 */
function google_tts_settings_page_html() {
    if (!current_user_can('manage_options')) {
        return;
    }
    ?>
    <div class="wrap">
        <h1><?php echo esc_html(get_admin_page_title()); ?></h1>
        <p>Nhập API Key của bạn cho dịch vụ Google Cloud Text-to-Speech.</p>
        <form action="options.php" method="post">
            <?php
            // In ra các trường cài đặt đã đăng ký
            settings_fields('google_tts_settings_group');
            // In ra các section của trang
            do_settings_sections('google-tts-settings-slug');
            // Nút lưu
            submit_button('Lưu thay đổi');
            ?>
        </form>
    </div>
    <?php
}

/**
 * Đăng ký setting, section, và field
 */
function google_tts_settings_init() {
    // 1. Đăng ký một setting mới, lưu với tên 'google_tts_api_key'
    register_setting('google_tts_settings_group', 'google_tts_api_key', [
        'type' => 'string',
        'sanitize_callback' => 'sanitize_text_field', // Làm sạch dữ liệu trước khi lưu
        'default' => ''
    ]);

    // 2. Thêm một section (nhóm các trường)
    add_settings_section(
        'google_tts_main_section',                // ID của section
        'Cài đặt API Key',                        // Tiêu đề section
        null,                                     // Hàm mô tả section (không cần)
        'google-tts-settings-slug'                // Slug của trang
    );

    // 3. Thêm trường nhập API Key vào section trên
    add_settings_field(
        'google_tts_api_key_field',               // ID của field
        'Google TTS API Key',                     // Tên hiển thị của field
        'google_tts_api_key_field_callback',      // Hàm để render input HTML
        'google-tts-settings-slug',               // Slug của trang
        'google_tts_main_section'                 // ID của section cha
    );
}
add_action('admin_init', 'google_tts_settings_init');

/**
 * Hàm render HTML cho trường nhập API Key
 */
function google_tts_api_key_field_callback() {
    $api_key = get_option('google_tts_api_key');
    ?>
    <input type="text" 
           name="google_tts_api_key" 
           value="<?php echo esc_attr($api_key); ?>" 
           class="regular-text"
           placeholder="Nhập API Key của bạn tại đây">
    <p class="description">
        API Key được lấy từ <a href="https://console.cloud.google.com/" target="_blank">Google Cloud Console</a>.
    </p>
    <?php
}

?>
